# Appunto 2026-03-18

## Fatto oggi

- Il codice precedente e' stato archiviato in `old/legacy_20260318/`.
- La root del progetto e' stata ripulita.
- E' stata creata una nuova base minima per il database in `forge_db.py`.
- Il bootstrap del database e' in `build_databases.py`.
- Il database runtime nuovo e' `data/cbforge.sqlite3`.
- I file JSON non sono piu' usati come database applicativo.
- Il file `input/normalized_account.json` resta solo come sorgente di import.
- Sono state create tabelle relazionali per:
  - catalogo campioni
  - skill campioni
  - stat base
  - campioni account
  - stat totali account
  - equip
  - substat equip
  - bonus account
  - set
  - combat
- E' stato creato `registry_report.py`.
- Il registry target ora considera solo i campioni posseduti a livello 60.
- Refresh policy salvata nel DB:
  - `registry_target_policy = owned_level_60_only`
- Ultimo refresh salvato in `app_state`.

## Stato attuale

- Target di enrichment: `114` campioni livello 60 unici.
- Dal dump attuale arrivano bene:
  - roster
  - equip
  - bonus account
  - nomi skill
- Dal dump attuale NON arrivano ancora bene:
  - cooldown base strutturati
  - cooldown booked strutturati
  - effetti skill strutturati

## Scoperta importante

- HellHades espone un endpoint utile:
  - `https://hellhades.com/wp-json/hh-api/v3/raid/skills/{postID}`
- Questo endpoint restituisce almeno:
  - nome skill
  - tipo skill
  - descrizione
  - cooldown base
  - books
- Esempio verificato su Geomancer:
  - `postID = 17837`
  - l'endpoint risponde correttamente

## Da fare domani

1. Costruire un importer HellHades -> SQLite per i campioni target livello 60.
2. Aggiungere tabelle o campi di enrichment per:
   - `cooldown_base`
   - `cooldown_booked`
   - `skill_type`
   - `description_clean`
   - effetti strutturati
3. Estrarre i book level dalla descrizione skill quando il campo `books` non basta.
4. Mappare gli effetti importanti per Clan Boss:
   - unkillable
   - block debuffs
   - counterattack
   - ally protect
   - increase speed
   - turn meter fill/reduce
   - decrease attack
   - decrease defense
   - weaken
   - poison
   - hp burn
   - shield
   - revive
5. Aggiornare `registry_report.py` per mostrare la copertura reale dei campioni livello 60.
6. Solo dopo: riprendere candidate selector e simulatore Clan Boss.

## Comandi utili

- Ricostruire il DB:
  - `python build_databases.py`
- Vedere lo stato del registry:
  - `python registry_report.py`
- Lanciare i test della base nuova:
  - `pytest -q`
